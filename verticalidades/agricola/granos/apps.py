from django.apps import AppConfig

class GranosConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'verticalidades.agricola.granos'
    verbose_name = 'Agrícola (Granos)'
